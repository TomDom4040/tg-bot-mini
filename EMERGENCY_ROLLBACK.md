# 🚨 ЭКСТРЕННЫЙ ОТКАТ - ИНСТРУКЦИЯ

## ⚡ БЫСТРЫЙ ОТКАТ (1 команда)

Если что-то пошло не так во время изменений:

```bash
cd /root/Tg_BOTs/tg-bot-mini && ./rollback.sh rollback
```

## 🔍 ДИАГНОСТИКА ПРОБЛЕМЫ

Если не знаете, что сломалось:

```bash
cd /root/Tg_BOTs/tg-bot-mini && ./rollback.sh diagnose
```

## 🏥 ПРОВЕРКА ЗДОРОВЬЯ

Проверить, работает ли бот нормально:

```bash
cd /root/Tg_BOTs/tg-bot-mini && ./health-check.sh
```

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### 1. Проверьте, что сломалось
```bash
./rollback.sh diagnose
```

### 2. Если бот не отвечает
```bash
./rollback.sh rollback
```

### 3. Проверьте восстановление
```bash
curl -I http://165.232.139.129:3000
```

### 4. Если не помогло
```bash
./restore.sh latest
```

## 🆘 ЭКСТРЕННЫЕ КОМАНДЫ

### Остановить бота
```bash
pkill -f "node index.js"
```

### Запустить бота
```bash
cd /root/Tg_BOTs/tg-bot-mini && nohup node index.js > server.log 2>&1 &
```

### Проверить логи
```bash
tail -f /root/Tg_BOTs/tg-bot-mini/server.log
```

### Проверить статус
```bash
curl -I http://165.232.139.129:3000
```

## 📊 ПРОВЕРКИ ПОСЛЕ ОТКАТА

1. ✅ **HTTP ответ**: `curl -I http://165.232.139.129:3000`
2. ✅ **Админка**: Откройте http://165.232.139.129:3000
3. ✅ **Логи**: `tail -f /root/Tg_BOTs/tg-bot-mini/server.log`
4. ✅ **Процесс**: `ps aux | grep "node index.js"`

## 🎯 ЧТО ДЕЛАЕТ ОТКАТ

1. **Останавливает** сломанного бота
2. **Сохраняет** текущее состояние (на случай, если нужно)
3. **Восстанавливает** из последнего рабочего бэкапа
4. **Устанавливает** зависимости
5. **Запускает** бота
6. **Проверяет** работоспособность

## ⏱️ ВРЕМЯ ВОССТАНОВЛЕНИЯ

- **Быстрый откат**: 30-60 секунд
- **Полное восстановление**: 1-2 минуты
- **Проверка работоспособности**: 5-10 секунд

## 🛡️ БЕЗОПАСНОСТЬ

- Откат **НЕ УДАЛЯЕТ** данные пользователей
- Сохраняет **ВСЕ НАСТРОЙКИ** ботов
- Восстанавливает **ТОЧНОЕ СОСТОЯНИЕ** на момент бэкапа

---
*Система отката создана для максимальной надежности*


